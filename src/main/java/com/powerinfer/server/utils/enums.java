package com.powerinfer.server.utils;

public class enums {
    public enum KeyType {
        ssh,
        hf,
    }

    public enum Visibility {
        pub,
        pvt,
    }

    public enum Arch {
        llama,
        falcon,
        opt,
    }
}
