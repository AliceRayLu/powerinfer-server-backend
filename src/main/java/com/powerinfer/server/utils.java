package com.powerinfer.server;

public class utils {
    public enum KeyType {
        ssh,
        hf,
    }

    public enum Visibility {
        pub,
        pvt,
    }

    public enum Arch {
        llama,
        falcon,
        opt,
    }
}
